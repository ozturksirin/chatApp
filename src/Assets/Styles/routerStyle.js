import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    area: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    image: {
        width: 40,
        height: 40,
        borderRadius: 22,
    },
    userName: {
        color: '#fff',
        fontSize: 18,
        fontWeight: 'bold',
        paddingLeft: 10,
    },
});